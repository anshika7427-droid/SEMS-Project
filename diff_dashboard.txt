diff --git a/frontend/pages/dashboard.html b/frontend/pages/dashboard.html
index 4d7818f..e301822 100644
--- a/frontend/pages/dashboard.html
+++ b/frontend/pages/dashboard.html
@@ -188,14 +188,34 @@
           <div
             class="flex items-center gap-4 pl-4 border-l border-surface-container-high"
           >
-            <button
-              class="relative p-2 text-slate-600 hover:bg-surface-container-low rounded-full transition-all"
-            >
-              <span class="material-symbols-outlined">notifications</span>
-              <span
-                class="absolute top-2 right-2 w-2 h-2 bg-secondary rounded-full"
-              ></span>
-            </button>
+            <!-- Interactive Notifications Dropdown -->
+            <div class="relative">
+              <button
+                id="notificationBell"
+                class="relative p-2 text-slate-600 hover:bg-slate-100 rounded-full transition-all flex items-center justify-center"
+                title="Notifications"
+              >
+                <span class="material-symbols-outlined">notifications</span>
+                <span
+                  id="notificationBadge"
+                  class="absolute top-1.5 right-1.5 min-w-[16px] h-[16px] px-1 bg-error text-white rounded-full text-[9px] font-bold flex items-center justify-center hidden border border-white"
+                >0</span>
+              </button>
+              
+              <!-- Notifications Popover -->
+              <div
+                id="notificationPopover"
+                class="absolute right-0 mt-3 w-80 bg-white border border-slate-100 rounded-2xl shadow-xl z-50 hidden flex flex-col glass-card overflow-hidden animate-in slide-in-from-top-2 duration-150"
+              >
+                <div class="px-4 py-3 border-b border-slate-100 flex items-center justify-between bg-slate-50/50">
+                  <span class="font-headline font-bold text-slate-700 text-xs uppercase tracking-wider">Notifications</span>
+                  <button id="markAllReadBtn" class="text-[10px] text-primary font-bold hover:underline">Mark all read</button>
+                </div>
+                <div id="notificationList" class="max-h-64 overflow-y-auto no-scrollbar divide-y divide-slate-100">
+                  <!-- Dynamic notification list items -->
+                </div>
+              </div>
+            </div>
             <a
               href="/profile"
               class="w-10 h-10 rounded-full overflow-hidden border-2 border-primary/10 hover:border-primary transition-all block animate-in fade-in duration-300"
@@ -380,42 +400,59 @@
           class="md:col-span-4 glass-card rounded-2xl p-8 shadow-sm border border-white/40 bg-gradient-to-br from-white/80 to-primary/5"
         >
           <h3
-            class="text-sm font-bold uppercase tracking-widest text-primary mb-6"
+            class="text-sm font-bold uppercase tracking-widest text-primary mb-4"
           >
             AI Grade Predictor
           </h3>
-          <div class="space-y-6">
+          <div class="space-y-4">
             <div
-              class="flex items-center justify-between p-4 bg-white/50 rounded-xl"
+              class="flex items-center justify-between p-3 bg-white/50 rounded-xl border border-white/60"
             >
-              <span class="text-on-surface-variant font-medium"
+              <span class="text-slate-600 text-xs font-semibold uppercase"
                 >Current Grade</span
               >
-              <span class="text-2xl font-headline font-bold text-on-surface" id="currentGradeText"
+              <span class="text-xl font-headline font-extrabold text-slate-800" id="currentGradeText"
                 >B+</span
               >
             </div>
             <div
-              class="relative py-6 flex flex-col items-center justify-center bg-primary rounded-2xl text-white shadow-lg shadow-primary/20 overflow-hidden"
+              class="relative p-4 flex flex-col items-center justify-center bg-primary rounded-2xl text-white shadow-lg shadow-primary/20 overflow-hidden"
             >
-              <div class="absolute top-0 right-0 p-2 opacity-20">
-                <span class="material-symbols-outlined text-6xl"
-                  >trending_up</span
+              <div class="absolute top-0 right-0 p-2 opacity-15">
+                <span class="material-symbols-outlined text-5xl"
+                  >insights</span
                 >
               </div>
-              <span class="text-xs uppercase tracking-widest opacity-80 mb-1"
-                >Predicted Finale</span
+              <span class="text-[10px] uppercase tracking-widest opacity-80 mb-1"
+                >Predicted Semester Grade</span
               >
               <div class="flex items-center gap-2">
-                <span class="text-5xl font-headline font-black" id="predictedGradeText">A-</span>
-                <span class="material-symbols-outlined text-3xl font-bold" id="predictedGradeArrow"
+                <span class="text-4xl font-headline font-black" id="predictedGradeText">A-</span>
+                <span class="material-symbols-outlined text-2xl font-bold" id="predictedGradeArrow"
                   >arrow_upward</span
                 >
               </div>
+              <div class="mt-1 text-[11px] font-semibold opacity-90" id="gradeConfidenceText">Confidence: 82%</div>
             </div>
-            <p class="text-xs text-center text-on-surface-variant italic px-2" id="gradePredictorTip">
-              Increase study time for "Physics Lab" by 15% to secure this
-              projection.
+            
+            <!-- Strengths and Risks Container -->
+            <div class="space-y-3 mt-2">
+              <div>
+                <h4 class="text-[10px] font-bold uppercase tracking-wider text-slate-400 mb-1">Strengths</h4>
+                <div id="gradeStrengthsList" class="space-y-1 text-xs">
+                  <!-- Dynamic strengths -->
+                </div>
+              </div>
+              <div>
+                <h4 class="text-[10px] font-bold uppercase tracking-wider text-slate-400 mb-1">Risks</h4>
+                <div id="gradeRisksList" class="space-y-1 text-xs">
+                  <!-- Dynamic risks -->
+                </div>
+              </div>
+            </div>
+
+            <p class="text-[11px] text-center text-on-surface-variant italic leading-tight" id="gradePredictorTip">
+              Complete tasks and keep your schedule active to raise your grade prediction.
             </p>
           </div>
         </div>
@@ -426,12 +463,17 @@
           <div class="flex justify-between items-center mb-8">
             <h3
               class="text-sm font-bold uppercase tracking-widest text-primary"
+              id="scheduleDayHeader"
             >
               Today's Schedule
             </h3>
-            <span class="material-symbols-outlined text-outline"
-              >calendar_today</span
+            <button
+              id="viewFullWeekBtn"
+              class="px-3 py-1.5 bg-primary/10 hover:bg-primary text-primary hover:text-white rounded-full text-xs font-bold transition-all flex items-center gap-1"
             >
+              <span class="material-symbols-outlined text-sm">calendar_view_week</span>
+              View Week
+            </button>
           </div>
           <div class="relative space-y-12 pl-6" id="todayScheduleContainer">
             <div
@@ -851,53 +893,64 @@
             }
 
             // 3. AI Grade Predictor Update
-            let currentScore = 75; // Default B
-            if (data.total_tasks > 0) {
-              currentScore = (data.completed_tasks / data.total_tasks) * 100;
-            }
-            const currentGrade = getGradeFromScore(currentScore);
             const currentGradeText = document.getElementById("currentGradeText");
-            if (currentGradeText) {
-              currentGradeText.textContent = currentGrade;
+            if (currentGradeText && data.current_grade) {
+              currentGradeText.textContent = data.current_grade;
             }
-            
-            // Set default predicted score to currentScore
-            let predictedScore = currentScore;
-            try {
-              const analysisRes = await fetch("/api/schedule/analysis?t=" + new Date().getTime(), { cache: "no-store" });
-              if (analysisRes.ok) {
-                const analysisData = await analysisRes.json();
-                if (analysisData.quality_scoring && analysisData.quality_scoring.exam_readiness_score !== undefined) {
-                  predictedScore = analysisData.quality_scoring.exam_readiness_score;
-                }
-              }
-            } catch (err) {
-              console.error("Error loading schedule analysis for grade predictor", err);
-            }
-            
-            const predictedGrade = getGradeFromScore(predictedScore);
+
             const predictedGradeText = document.getElementById("predictedGradeText");
-            if (predictedGradeText) {
-              predictedGradeText.textContent = predictedGrade;
+            if (predictedGradeText && data.predicted_grade) {
+              predictedGradeText.textContent = data.predicted_grade;
             }
-            
+
+            const gradeConfidenceText = document.getElementById("gradeConfidenceText");
+            if (gradeConfidenceText && data.grade_confidence !== undefined) {
+              gradeConfidenceText.textContent = `Confidence: ${data.grade_confidence}%`;
+            }
+
             const predictedGradeArrow = document.getElementById("predictedGradeArrow");
-            if (predictedGradeArrow) {
-              if (predictedScore > currentScore) {
+            if (predictedGradeArrow && data.predicted_score !== undefined && data.current_score !== undefined) {
+              if (data.predicted_score > data.current_score) {
                 predictedGradeArrow.textContent = "arrow_upward";
-                predictedGradeArrow.className = "material-symbols-outlined text-3xl font-bold text-green-500 animate-bounce";
-              } else if (predictedScore < currentScore) {
+                predictedGradeArrow.className = "material-symbols-outlined text-2xl font-bold text-green-300 animate-bounce";
+              } else if (data.predicted_score < data.current_score) {
                 predictedGradeArrow.textContent = "arrow_downward";
-                predictedGradeArrow.className = "material-symbols-outlined text-3xl font-bold text-red-500";
+                predictedGradeArrow.className = "material-symbols-outlined text-2xl font-bold text-red-300";
               } else {
                 predictedGradeArrow.textContent = "trending_flat";
-                predictedGradeArrow.className = "material-symbols-outlined text-3xl font-bold text-slate-400";
+                predictedGradeArrow.className = "material-symbols-outlined text-2xl font-bold text-slate-300";
               }
             }
-            
+
+            const strengthsList = document.getElementById("gradeStrengthsList");
+            if (strengthsList && data.grade_strengths) {
+              strengthsList.innerHTML = data.grade_strengths.map(s => `
+                <div class="flex items-center gap-1.5 text-green-600 font-semibold">
+                  <span class="material-symbols-outlined text-[14px]">check_circle</span>
+                  <span>${s}</span>
+                </div>
+              `).join("");
+            }
+
+            const risksList = document.getElementById("gradeRisksList");
+            if (risksList && data.grade_risks) {
+              risksList.innerHTML = data.grade_risks.map(r => {
+                let colorClass = "text-amber-600";
+                if (r.includes("unprepared") || r.includes("Low task")) {
+                  colorClass = "text-red-500 font-bold";
+                }
+                return `
+                  <div class="flex items-center gap-1.5 ${colorClass} font-semibold">
+                    <span class="material-symbols-outlined text-[14px]">warning</span>
+                    <span>${r}</span>
+                  </div>
+                `;
+              }).join("");
+            }
+
             const gradePredictorTip = document.getElementById("gradePredictorTip");
-            if (gradePredictorTip) {
-              gradePredictorTip.textContent = data.focus_insight || "Complete tasks and keep your schedule active to raise your grade prediction.";
+            if (gradePredictorTip && data.grade_tip) {
+              gradePredictorTip.textContent = data.grade_tip;
             }
           }
         } catch (e) {
@@ -939,6 +992,11 @@
             const dayNames = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
             const todayName = dayNames[new Date().getDay()];
             
+            const dayHeader = document.getElementById("scheduleDayHeader");
+            if (dayHeader) {
+              dayHeader.textContent = `${todayName}'s Schedule`;
+            }
+            
             const todayEvents = events.filter(e => e.day_of_week && e.day_of_week.trim().toLowerCase() === todayName.toLowerCase());
             
             if (todayEvents.length === 0) {
@@ -1006,6 +1064,211 @@
         }
       }
 
+      // Open and populate weekly timetable modal
+      async function setupWeeklyModal() {
+        const modal = document.getElementById("weeklyTimetableModal");
+        const openBtn = document.getElementById("viewFullWeekBtn");
+        const closeBtn = document.getElementById("closeWeeklyModalBtn");
+        
+        if (openBtn && modal) {
+          openBtn.addEventListener("click", async () => {
+            modal.classList.remove("hidden");
+            await populateWeeklyGrid();
+          });
+        }
+        
+        if (closeBtn && modal) {
+          closeBtn.addEventListener("click", () => {
+            modal.classList.add("hidden");
+          });
+          // Close on outside click
+          modal.addEventListener("click", (e) => {
+            if (e.target === modal) {
+              modal.classList.add("hidden");
+            }
+          });
+        }
+      }
+
+      async function populateWeeklyGrid() {
+        try {
+          const res = await fetch("/api/schedule/all?t=" + new Date().getTime(), {
+            cache: "no-store"
+          });
+          if (res.ok) {
+            const events = await res.json();
+            const grid = document.getElementById("modalWeeklyGrid");
+            if (!grid) return;
+            
+            grid.innerHTML = "";
+            const days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];
+            
+            days.forEach(day => {
+              const dayEvents = events.filter(e => e.day_of_week && e.day_of_week.trim().toLowerCase() === day.toLowerCase());
+              dayEvents.sort((a, b) => a.start_time.localeCompare(b.start_time));
+              
+              let dayBlocks = "";
+              if (dayEvents.length === 0) {
+                dayBlocks = `
+                  <div class="py-4 text-center text-slate-400 text-xs italic bg-slate-50 rounded-xl border border-slate-100">
+                    Rest Day
+                  </div>
+                `;
+              } else {
+                dayEvents.forEach(event => {
+                  const diff = (event.subject_difficulty || "Medium").toLowerCase().trim();
+                  let bgClass = "bg-amber-50 border-amber-200 text-amber-800";
+                  if (diff === "hard") {
+                    bgClass = "bg-red-50 border-red-200 text-red-800";
+                  } else if (diff === "easy") {
+                    bgClass = "bg-green-50 border-green-200 text-green-800";
+                  }
+                  dayBlocks += `
+                    <div class="p-2.5 rounded-xl border ${bgClass} text-[11px] leading-tight">
+                      <div class="font-bold mb-0.5">${formatTime12Hour(event.start_time)}</div>
+                      <div class="font-extrabold uppercase truncate">${event.subject_name}</div>
+                      <div class="text-[9px] opacity-75 font-semibold mt-0.5">${event.session_type || 'Study'}</div>
+                    </div>
+                  `;
+                });
+              }
+              
+              // Highlight today
+              const isToday = day.toLowerCase() === new Date().toLocaleString('en-us', {weekday: 'long'}).toLowerCase();
+              const todayBgClass = isToday ? "bg-primary/5 border-primary/20 ring-2 ring-primary/20" : "bg-white border-slate-100";
+              const todayHeaderClass = isToday ? "text-primary font-black" : "text-slate-700 font-bold";
+              
+              grid.innerHTML += `
+                <div class="flex flex-col gap-3 p-4 rounded-2xl border ${todayBgClass}">
+                  <h4 class="text-center text-xs uppercase tracking-wider ${todayHeaderClass}">${day.slice(0,3)}</h4>
+                  <div class="flex flex-col gap-2">
+                    ${dayBlocks}
+                  </div>
+                </div>
+              `;
+            });
+          }
+        } catch (e) {
+          console.error("Error loading weekly timetable", e);
+        }
+      }
+
+      // Notifications Setup and Event Listeners
+      async function setupNotifications() {
+        const bell = document.getElementById("notificationBell");
+        const popover = document.getElementById("notificationPopover");
+        const markAllBtn = document.getElementById("markAllReadBtn");
+        
+        if (bell && popover) {
+          bell.addEventListener("click", async (e) => {
+            e.stopPropagation();
+            popover.classList.toggle("hidden");
+            if (!popover.classList.contains("hidden")) {
+              await loadNotificationList();
+            }
+          });
+          
+          document.addEventListener("click", (e) => {
+            if (!popover.contains(e.target) && !bell.contains(e.target)) {
+              popover.classList.add("hidden");
+            }
+          });
+        }
+        
+        if (markAllBtn) {
+          markAllBtn.addEventListener("click", async () => {
+            try {
+              const res = await fetch("/api/notifications/read-all", { method: "PUT" });
+              if (res.ok) {
+                await updateNotificationBadge();
+                await loadNotificationList();
+              }
+            } catch (err) {
+              console.error("Error marking all read", err);
+            }
+          });
+        }
+        
+        // Initial badge update
+        await updateNotificationBadge();
+      }
+
+      async function updateNotificationBadge() {
+        try {
+          const res = await fetch("/api/notifications/unread-count?t=" + new Date().getTime());
+          if (res.ok) {
+            const data = await res.json();
+            const badge = document.getElementById("notificationBadge");
+            if (badge) {
+              if (data.count > 0) {
+                badge.textContent = data.count;
+                badge.classList.remove("hidden");
+              } else {
+                badge.classList.add("hidden");
+              }
+            }
+          }
+        } catch (err) {
+          console.error("Error updating notification badge", err);
+        }
+      }
+
+      async function loadNotificationList() {
+        try {
+          const res = await fetch("/api/notifications/?t=" + new Date().getTime());
+          if (res.ok) {
+            const list = await res.json();
+            const container = document.getElementById("notificationList");
+            if (!container) return;
+            
+            if (list.length === 0) {
+              container.innerHTML = `
+                <div class="p-8 text-center text-slate-400 text-xs font-semibold">
+                  <span class="material-symbols-outlined text-3xl mb-1 text-slate-300">notifications_off</span>
+                  <p>No new notifications</p>
+                </div>
+              `;
+              return;
+            }
+            
+            container.innerHTML = list.map(n => {
+              const bgClass = n.is_read ? "opacity-60 bg-white" : "bg-primary/5 font-semibold";
+              const checkmarkBtn = n.is_read ? "" : `
+                <button onclick="markNotificationRead(${n.id}, event)" class="p-1 hover:bg-slate-200 rounded text-slate-500 hover:text-primary transition-all flex items-center justify-center" title="Mark as read">
+                  <span class="material-symbols-outlined text-[16px]">done</span>
+                </button>
+              `;
+              return `
+                <div class="px-4 py-3 flex items-start justify-between gap-3 ${bgClass} transition-all">
+                  <div class="flex-1 min-w-0">
+                    <p class="text-xs text-slate-800 leading-snug">${n.title}</p>
+                    <p class="text-[11px] text-slate-500 leading-tight mt-0.5 whitespace-pre-line">${n.message}</p>
+                  </div>
+                  ${checkmarkBtn}
+                </div>
+              `;
+            }).join("");
+          }
+        } catch (err) {
+          console.error("Error loading notifications list", err);
+        }
+      }
+
+      async function markNotificationRead(id, event) {
+        if (event) event.stopPropagation();
+        try {
+          const res = await fetch(`/api/notifications/read/${id}`, { method: "PUT" });
+          if (res.ok) {
+            await updateNotificationBadge();
+            await loadNotificationList();
+          }
+        } catch (err) {
+          console.error("Error marking notification read", err);
+        }
+      }
+
+      window.markNotificationRead = markNotificationRead;
+
       // Smooth Scrolling
       document.querySelectorAll("a").forEach((anchor) => {
         anchor.addEventListener("click", function (e) {
@@ -1024,7 +1287,33 @@
         await loadTasks();
         await loadDashboardStats();
         await loadTodaySchedule();
+        await setupWeeklyModal();
+        await setupNotifications();
       };
     </script>
+    <!-- Weekly Timetable Modal -->
+    <div id="weeklyTimetableModal" class="fixed inset-0 z-50 hidden flex items-center justify-center p-4 bg-slate-900/60 backdrop-blur-sm animate-in fade-in duration-200">
+      <div class="bg-white/95 border border-white/40 rounded-3xl max-w-6xl w-full max-h-[90vh] overflow-hidden shadow-2xl flex flex-col glass-card">
+        <!-- Modal Header -->
+        <div class="px-8 py-6 border-b border-slate-100 flex items-center justify-between">
+          <div class="flex items-center gap-3">
+            <span class="material-symbols-outlined text-primary text-3xl">calendar_view_week</span>
+            <div>
+              <h3 class="text-xl font-headline font-bold text-slate-800">Weekly Study Timetable</h3>
+              <p class="text-xs text-slate-500 font-medium">Your optimized study distribution for the week</p>
+            </div>
+          </div>
+          <button id="closeWeeklyModalBtn" class="p-2 hover:bg-slate-100 rounded-full text-slate-500 hover:text-slate-800 transition-all">
+            <span class="material-symbols-outlined">close</span>
+          </button>
+        </div>
+        <!-- Modal Body -->
+        <div class="p-8 overflow-y-auto max-h-[70vh] no-scrollbar">
+          <div class="grid grid-cols-1 md:grid-cols-7 gap-4" id="modalWeeklyGrid">
+            <!-- Weekly grid columns populated dynamically -->
+          </div>
+        </div>
+      </div>
+    </div>
   </body>
 </html>
